import styled from 'styled-components';

const SAbout = styled.div`
  .content {
    width: 100%;
    padding: 0 10%;
    text-align: center;

    p {
      font-size: 16px;
    }
  }
`;

export default SAbout;
